<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.simulando">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- App Activities -->
        <activity
            android:name="com.simulando.UI.Splash.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.simulando.UI.Dashboard.DashboardActivity"
            android:label="@string/home_label"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="com.simulando.UI.Dashboard.AnswerQuestions.AnswerQuestionsActivity"
            android:parentActivityName="com.simulando.UI.Dashboard.DashboardActivity">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.simulando.UI.Dashboard.DashboardActivity" />
        </activity>

        <activity
            android:name="com.simulando.UI.Intro.IntroActivity"
            android:theme="@style/AppTheme.NoTitle" />

        <activity
            android:name="com.simulando.UI.Login.LoginActivity"
            android:label="@string/login_label"
            android:launchMode="singleTop"
            android:parentActivityName="com.simulando.UI.Intro.IntroActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.simulando.UI.Intro.IntroActivity" />
        </activity>
        <activity
            android:name="com.simulando.UI.Register.RegisterActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/register_label"
            android:launchMode="singleTop"
            android:parentActivityName="com.simulando.UI.Intro.IntroActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.simulando.UI.Intro.IntroActivity" />
        </activity>

        <!-- Third-party Activities -->
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            tools:replace="android:theme" />

        <!-- Third-party Metadata -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />


    </application>

</manifest>